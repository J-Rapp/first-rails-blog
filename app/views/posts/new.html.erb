<h1>New Post!!!</h1>

<%= form_for :post, url: posts_path do |form| %>
	<%= form.label :title %><br>
	<%= form.text_field :title %><br>

	<%= form.label :body %><br>
	<%= form.text_area :body %><br>

	<%= form.submit %>
<% end %>